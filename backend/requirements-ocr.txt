# Optional, heavy. Needed for scanned PDFs and photo-of-a-page uploads.
# Install separately: pip install -r requirements-ocr.txt
# Requires Python 3.12 or older — paddlepaddle only ships wheels up to
# cp312 as of this writing. Without it, Bodhi still runs: copy-paste PDFs
# work fully; scanned/photo uploads will return a clear error until this
# is installed.
paddlepaddle
paddleocr
